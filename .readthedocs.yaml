version: 2

# Build from the docs/ directory with Sphinx
sphinx:
  configuration: src/docs/conf.py

# Explicitly set the version of Python and its requirements
python: 
  install:
    - requirements: src/docs/doc-requirements.txt